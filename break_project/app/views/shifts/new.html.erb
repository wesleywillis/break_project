<%= form_for @shift, url: { action: @action}, method: @method do |f| %>

  <fieldset>
    <legend></legend>
    <br>
    <%= f.label @label, @header %>
    <%= f.check_box @label%>`
    <br>
  </fieldset>
  <%= f.submit %>
<% end %>
